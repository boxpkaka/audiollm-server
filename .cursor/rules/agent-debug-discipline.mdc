---
description: Agent 调试与重构纪律：日志先于代码、矩阵覆盖、去重先行
alwaysApply: true
---

# Agent 调试与重构纪律

本文件由真实事故复盘提炼，每条规则后都标了触发它的事故，方便理解动机。

## 1. 现场问题：日志先于代码

用户报"慢/不工作/没响应"类问题时，第一动作必须是抓 runtime evidence（日志、监控、复现），再读代码/配置。**不要先去 grep 配置**。

```bash
journalctl -u audiollm-demo --since "5 minutes ago" --no-pager | rg "session|vad|httpx" | tail -50
```

事故：用户问"长时间没触发 final"。日志里 32 秒只发 partial、零 final，已经直接给答案；先抓日志能省一半时间。

## 2. 解耦布尔开关：先画完整 N×N 矩阵

任何把"一个开关"拆成"多个开关"的改动，必须先列完整的配置组合 × 行为矩阵；矩阵覆盖率是评审硬指标，不允许"漏掉一种合法组合"。

事故：把 `enable_secondary_asr` 解耦成 `secondary + fusion`，第一次实现把 partial 守卫从 `(PSEUDO and SECONDARY)` 换成 `(PSEUDO and PRIMARY)` — 锁死的对象换了，但"partial 数据源是 OR 关系"这一点仍然没被照顾到，`primary=false, secondary=true` 这种合法组合不出 partial。

模板：

| 开关 A | 开关 B | 期望行为 | 当前实现 |
|---|---|---|---|
| true | true | ... | ... |
| true | false | ... | ... |
| false | true | ... | ... |
| false | false | ... | ... |

只要有一行没被代码或测试覆盖，就不算改完。

## 3. 同语义双路径：先去重，再改

发现两条代码路径实现**同一个语义**（例如 `session.py` 与 `tasks/asr.py` 各自实现 partial 调度），应先抽公共函数再做功能改动；否则每改一次都要同步两边，必然漏一边。

事故：两条 partial 路径连续两次漏对齐：

- 第一次：legacy `session.py` 漏掉 `tasks/asr.py` 已支持的"primary off → secondary-only partial"分支。
- 第二次：legacy `session.py` 又一次漏掉相同的回退路径，原因是没去重。

判断信号：用 `Grep` 搜某个函数/语义出现≥2 个独立实现且参数面相似，就应该停下来去重。

## 4. 改完之前的自我交叉检查

提交多文件改动前回答三个问题：

1. 改动涉及的合法配置组合，每一种我都跑过或读过期望路径吗？
2. 如果有 legacy/new 双路径，我是不是只改了我熟悉的那条？
3. 我的"自我批判"列表里第一条暴露的根因，是不是被新代码绕过了，而不是真的修了？

事故：我第一次解耦 secondary 时，自我批判里写了"两路径不一致是 footgun"，但没动手去重，第二次改的时候立刻在同一个地方又栽。
