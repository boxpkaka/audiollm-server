---
description: 前后端严格分离 + 后端接口改动必须同步文档（强制）
alwaysApply: true
---

# 前后端接口契约纪律

本项目的前端 (`frontend/`) 与后端 (`backend/`) 仅通过 HTTP/WebSocket 协议通信。
任何破坏这一边界、或修改后端对外接口而未同步文档的改动，视为不完整改动。

## 1. 前后端分离的硬边界

禁止：

- 后端模板渲染前端页面（不引入 Jinja/SSR；前端是纯静态资产）
- 前端 JS 通过相对路径直接读 `backend/` 内文件
- 同一逻辑分别在前后端各写一份"约定字段名"（如 enum 值、错误码字符串），如必须共享请走 OpenAPI 自动生成或显式文档登记
- 后端在错误响应里塞 HTML（必须是 JSON `{message, code, detail}` 结构）

允许：

- 前端通过 `/api/...` REST 和 `/ws/...`、`/transcribe-streaming`、`/emotion-segmented-streaming` WebSocket 调用后端
- 前端通过 `fetch('/openapi.json')` 等公开端点拉取 schema

## 2. 什么算"后端接口修改"

以下任一变化都算接口改动，必须同步对应文档：

- 新增/删除/重命名 路由 (`@app.<method>` / `@app.websocket`)
- 请求/响应字段的增删改（含可选字段语义变化）
- WS 协议中任意 `type` 取值或字段语义变化（含 `partial` / `final` / `vad_event` 等）
- HTTP 状态码 / 错误结构（`detail` 子字段）变化
- 客户端可在 `start.config` 临时覆写的字段集合变化
- 默认值改变且对外可观察（如 `enable_pseudo_stream` 默认从 true 变 false）

不算（无需文档）：

- 内部 Python 函数签名、模块重组、私有辅助函数

## 3. 路由 ↔ 文档映射（修改时必须同步对应一行）

| 后端路由 (`backend/main.py`) | 文档 |
|---|---|
| `@app.websocket("/ws/audio")` | `docs/api-reference.md`（浏览器 demo 调试接口；外部集成不推荐） |
| `@app.websocket("/transcribe-streaming")` | `docs/transcribe-streaming-protocol.md` + `docs/api-reference.md` 速览 |
| `@app.websocket("/emotion-segmented-streaming")` | `docs/emotion-segmented-streaming-protocol.md` + `docs/api-reference.md` 速览 |
| `@app.post("/api/asr/upload")` | `docs/api-reference.md` |
| `@app.post("/api/asr/enrollment")` | `docs/api-reference.md` + `docs/transcribe-streaming-protocol.md` 注册接口章节 |
| `@app.delete("/api/asr/enrollment/{id}")` | 同上 |
| `@app.post("/api/audio/analyze")` | `docs/audio-analyze-api.md`（内部）+ `docs/public-audio-analyze-api.md`（对外） |
| `@app.post("/api/emotion/jobs")` + `@app.get("/api/emotion/jobs/{job_id}")` | `docs/api-reference.md` |
| `@app.post("/api/emotion-spec/jobs")` + `@app.get("/api/emotion-spec/jobs/{job_id}")` | 已知漂移，见第 6 节 |

新增路由时：必须在 `docs/api-reference.md` 至少添加一节，且更新此表（更新本规则文件视为允许变更，无需绕过 rule lock）。

## 4. 改后端接口的强制清单

提交前自检：

1. 改动是否在第 2 节列出的"接口修改"集合内？是 → 进入下面步骤
2. 我有没有在对应文档（第 3 节映射）补足：
   - 路由 + 方法
   - 请求字段（必选/可选/类型/示例）
   - 响应字段（必选/可选/类型/示例）
   - 错误码与 `detail` 结构
3. 如果前端要消费这个改动，前端代码（`frontend/app.js`、`frontend/streaming-text.js` 等）是否同步修改？
4. 是否更新了 `tests/` 里至少一个用例覆盖新行为？
5. 文档示例（`curl` / JSON 片段）是否可以原样跑通？

任何一项空缺即视为改动未完成。

## 5. 验证手段

提交前可以快速 grep 自检接口/文档是否漂移：

```bash
# 列出代码里所有路由，与文档 grep 对照
rg "@app\.(websocket|get|post|put|delete)" backend/ -n
rg "POST|GET|DELETE|WebSocket" docs/ -n
```

如果代码里有的路由 docs 里找不到，或 docs 里描述的字段代码里没有，就是契约漂移。

## 6. 已知漂移（写规则时的存量债务，新改动不允许扩大）

- `POST /api/emotion-spec/jobs` 与 `GET /api/emotion-spec/jobs/{job_id}` 在 `docs/` 里**完全无对应描述**。下次再动这两个路由时，必须先在 `docs/api-reference.md` 补一节，再做功能改动。

新的漂移点出现时请追加到本节，让后续 agent 能在改动前看到债务全景。
