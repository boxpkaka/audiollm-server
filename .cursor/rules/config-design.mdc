---
description: 配置/参数设计纪律：不变量下沉到数据源、禁止双参数取胜
alwaysApply: true
---

# 配置 / 参数设计纪律

本文件由真实事故复盘提炼。

## 1. 数据类不变量下沉到构造器，不靠外部函数

Python `dataclass` 的字段间依赖（不变量）应在 `__post_init__` 强制执行，不要依赖 `load_config()` / `override()` 等外部函数。后者无法覆盖"直接 `Config(...)` 构造"路径（典型在测试中）。

事故：第一次实现 `_enforce_asr_consistency(cfg)` 只在 load/override 调用，测试用 `Config(enable_secondary_asr=False)` 直接构造时不变量失效，导致 `enable_dual_asr_fusion` 仍为 `True`、副模型被意外调用。

```python
@dataclass(frozen=True)
class Config:
    enable_secondary_asr: bool = True
    enable_dual_asr_fusion: bool = True

    def __post_init__(self) -> None:
        if self.enable_dual_asr_fusion and not self.enable_secondary_asr:
            object.__setattr__(self, "enable_dual_asr_fusion", False)
```

`load_config` 这一层仍然保留 WARN 日志（让运维看见错误配置），但**降级逻辑只在 dataclass 里有一份**。

## 2. 禁止两个参数表达同一概念然后取 max/min

如果两个配置项语义重叠（同一物理量的不同表达），不允许在代码里用 `max(a, b)` 或 `min(a, b)` 让"哪个严格用哪个"。这会让用户改一个参数看不到效果，造成"配置欺骗"。

事故：`vad_end_frames` 与 `silence_duration_ms`（毫秒友好）都表达"切段所需的连续静音长度"，代码里 `end_threshold = max(silence_frames, end_frames)`。结果用户调 `silence_duration_ms: 200`（13 帧）完全无效，因为被 `end_frames: 25` 盖住。

正确做法（选一）：

- 删除其中一个（推荐）；
- 保留两个但明确"哪个是 fallback"、并在另一个上加 WARN 当被忽略时。

## 3. 配置项命名必须匹配其实际作用面

不要让一个开关名暗示一个语义，实际却控制三件事。

事故：`enable_secondary_asr` 字面上是"副模型是否在线"，但实际同时门控了：
- partial 通道是否开
- partial 是否做静音门
- final 是否做融合

正确做法：每个独立可观察的行为给一个独立开关，并在文档里给出 N×N 行为矩阵（参见 `agent-debug-discipline.mdc` 第 2 条）。

## 4. 新增/拆分配置时必须同步更新

修改 `backend/config.py` 字段必须同步：

- `backend/config.json` 默认值
- `.env.example` 环境变量注释
- `README.md` 与 `docs/api-reference.md` 中的字段表
- 涉及行为矩阵的，必须更新矩阵
- 至少一个 pytest 用例覆盖新的不变量
